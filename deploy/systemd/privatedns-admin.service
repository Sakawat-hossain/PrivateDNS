[Unit]
Description=PrivateDNS operator dashboard
Documentation=https://github.com/Sakawat-hossain/PrivateDNS
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=privatedns
Group=privatedns
ExecStart=/usr/local/bin/privatedns-admin -config /etc/private-dns/admin.yaml
Restart=always
RestartSec=3

# Binds a high loopback port, so no capabilities are needed. This service is
# reached over a VPN or an SSH tunnel rather than exposed.
NoNewPrivileges=true
PrivateTmp=true
PrivateDevices=true
ProtectSystem=strict
ProtectHome=true
ProtectKernelTunables=true
ProtectKernelModules=true
ProtectControlGroups=true
ProtectHostname=true
ProtectClock=true
RestrictNamespaces=true
RestrictRealtime=true
RestrictSUIDSGID=true
RestrictAddressFamilies=AF_INET AF_INET6 AF_UNIX
LockPersonality=true
MemoryDenyWriteExecute=true
SystemCallArchitectures=native
SystemCallFilter=@system-service
SystemCallFilter=~@privileged @resources

ReadWritePaths=/var/lib/private-dns
ReadOnlyPaths=/etc/private-dns

LimitNOFILE=8192

[Install]
WantedBy=multi-user.target
