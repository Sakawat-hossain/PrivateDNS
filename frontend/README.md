# Frontend

Not yet implemented.

Two separate interfaces will live here, both talking to the backend over its
API and neither touching the resolver directly:

- **Admin dashboard** — server status, statistics, customer and tenant
  management, policy editors, API tokens, audit log.
- **Customer portal** — the tenant hostname, one-tap source-IP registration,
  platform setup guides, expiry and renewal, a pause-filtering control.
